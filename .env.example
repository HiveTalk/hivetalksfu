HIVETALK_URL=https://vanilla.hivetalk.org

# API endpoint for fetching balance information
ZAP_GOAL_API_URL=https://zapgoalsvr.vercel.app/api/balance

# Zap goal threshold in sats
# Used for both blocking room access and displaying the goal
ZAP_GOAL_SATS=60000

# Default zap amount (in sats)
# Pre-filled amount when users click the "Zap" button
ZAP_DEFAULT_AMOUNT=2100

# ZBD API Key for payment processing
ZBD_API_KEY=

# Room Lock Price in Sats
ROOM_LOCK_PRICE_SATS=1000

# How long a room lock persists after all peers leave (in hours, default: 24)
ROOM_LOCK_TTL_HOURS=24
